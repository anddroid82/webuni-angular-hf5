import { InfectionDirective } from './infection.directive';

describe('InfectionDirective', () => {
  it('should create an instance', () => {
    const directive = new InfectionDirective();
    expect(directive).toBeTruthy();
  });
});
